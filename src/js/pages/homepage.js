export default class Homepage {
  constructor (pageId) {
    this.scope = document.getElementById(pageId)

    this.init()
  }

  init () {
    if (this.scope) {
      // Put your code here
    }
  }
}
